<%= f.fields_for :detail_ordonnances do |detail| %>
  <div class="col-sm-4"><%= detail.collection_select :medicament_id, Medicament.all, :id, :nom,  include_blank: true %></div>
  <div class="col-sm-4"><%= detail.text_field :quantite %></div>
  <div class="col-sm-4"><%= detail.text_field :prix_unitaire %></div>
<% end %>
