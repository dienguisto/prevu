<div class="wrapper wrapper-content animated fadeInRight">
  <div class="row">
    <div class="col-lg-12">
      <div class="text-center m-t-lg">
        <h1>
          Bienvenue dans Prevu Server
        </h1>
        <small>
          <%= current_user.profil %>
        </small>
      </div>
    </div>
  </div>
</div>

<% if current_user.user_system? %>
    <%= render 'dashboard/system' %>
<% end %>
<% if current_user.user_structure_assurance? %>
    <%= render 'dashboard/structure_assurance' %>
<% end %>
<% if current_user.user_formation_sanitaire? %>
    <%= render 'dashboard/formation_sanitaire' %>
<% end %>
<% if current_user.user_pharmacie? %>
    <%= render 'dashboard/pharmacy' %>
<% end %>
<% if current_user.user_structure_aperitrice? %>
    <%= render 'dashboard/copie' %>
<% end %>