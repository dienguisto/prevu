<p id="notice"><%= notice %></p>

<h1>Listing formation_sanitaires</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @formation_sanitaires.each do |formation_sanitaire| %>
      <tr>
        <td><%= link_to 'Show', formation_sanitaire %></td>
        <td><%= link_to 'Edit', edit_formation_sanitaire_path(pharmacy) %></td>
        <td><%= link_to 'Destroy', formation_sanitaire, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New formation_sanitaire', new_formation_sanitaire_path %>
