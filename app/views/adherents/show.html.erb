<%= titre_page "Adhérent : #{@adherent.matricule} #{"(ticket modérateur : #{@adherent.souscriptions.en_cours.last.formule.ticket_moderateur} %)" unless @adherent.souscriptions.en_cours.empty?} #{"(#{Adherent::AFFILIATION.index(@adherent.affiliation)} de #{link_to @adherent.parrain.full_name, @adherent.parrain})" unless @adherent.parrain.nil?}" %>

<div class="wrapper wrapper-content">
  <div class="row">
    <div class="col-md-12">
      <%= render 'adherents/alerte_cotisation' %>
    </div>
    <div class="col-md-4">
      <%= render 'adherents/details' %>
    </div>

    <div class="col-md-8">
      <%= render 'adherents/souscriptions' %>
      <%= render 'adherents/versements' %>
      <%= render 'adherents/ordonnances' %>
      <%= render 'adherents/consultations' %>
      <%= render 'adherents/contacts' %>
      <%= render 'adherents/filleuls' %>
    </div>
  </div>
</div>

<%= render 'adherents/modal' %>
