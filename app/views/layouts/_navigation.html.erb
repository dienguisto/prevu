<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="sidebar-collapse">
    <ul class="nav" id="side-menu">
      <li class="nav-header">
        <div class="dropdown profile-element">
					<span>
						<% if current_formation_sanitaire or current_pharmacy %>
							<img alt="image" class="img-circle" src="<%= image_path(current_user.entite.entite.structure_sanitaire.logo.url(:thumb)) %>" />
						<% elsif current_structure_assurance %>
							<img alt="image" class="img-circle" src="<%= image_path(current_user.entite.entite.structure_assurance.logo.url(:thumb)) %>" />
						<%else %>
							<img alt="image" class="img-circle" src="" />
						<% end %>
					</span>
          <a href="#">
            <span class="clear">
                <span class="block m-t-xs">
                    <strong class="font-bold"><%= current_user.email %></strong> <br/>
                  <%= current_user.entite.try :title %>
                </span>
            </span>
          </a>
        </div>
        <div class="logo-element">
          PREVU
        </div>
      </li>
      <% if user_signed_in? %>
          <li>
            <%= link_to root_path do %>
                <i class="fa fa-dashboard"></i> <span class="nav-label">Tableau de bord</span>
            <% end %>
          </li>
          <% if current_user.user_system? %>
              <%= render 'layouts/menu_system' %>
          <% end %>
          <% if current_user.user_mutuelle? %>
              <%= render 'layouts/menu_mutuelle' %>
          <% end %>
          <% if current_user.user_micro_assurance? %>
              <%= render 'layouts/menu_micro_assurance' %>
          <% end %>
          <% if current_user.user_formation_sanitaire? %>
              <%= render 'layouts/menu_formation_sanitaire' %>
          <% end %>
          <% if current_user.user_pharmacie? %>
              <%= render 'layouts/menu_pharmacie' %>
          <% end %>
          <% if current_user.user_structure_aperitrice? %>
              <%= render 'layouts/menu_structure_aperitrice' %>
          <% end %>
      <% end %>
    </ul>

  </div>
</nav>