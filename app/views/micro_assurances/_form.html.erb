<%= simple_form_for(@micro_assurance) do |f| %>
    <%= f.error_notification %>

    <%= render 'structure_assurances/form', f: f %>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
<% end %>
