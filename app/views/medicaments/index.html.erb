<%= titre_page 'Liste des Medicaments' %>

<div class="wrapper wrapper-content">
  <div class="row">
    <div class="col-lg-12">
      <div class="ibox float-e-margins">
        <div class="ibox-content">
          <div class="row">
            <div class="col-sm-12">
              <div class="actions_bouton">
                <%= link_to '<i class="fa fa-plus"></i> Ajouter'.html_safe, new_medicament_path, class: 'btn btn-primary' %>
              </div>
            </div>
          </div>
          <div class="table-responsive">
            <table class="table table-striped table-hover">
              <thead>
                <tr>
                  <th width="10px"></th>
                  <th>Nom</th>
                  <th>Reference</th>
                  <th>Actif?</th>
                  <th>Date ajout</th>
                  <th colspan="2"></th>
                </tr>
              </thead>

              <tbody>
                <% @medicaments.each do |medicament| %>
                  <tr>
                    <td><%= link_to '#'+medicament.id.to_s, medicament %></td>
                    <td><%= medicament.nom %></td>
                    <td><%= medicament.reference %></td>
                    <td><%= afficher_etat(medicament.actif) %></td>
                    <td><%= medicament.created_at %></td>
                    <td width="50px"><%= link_to 'Edit', edit_medicament_path(medicament) %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>

        </div>
      </div>
    </div>

  </div>
</div>